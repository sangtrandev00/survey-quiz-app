# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build survey-quiz-app`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t survey-quiz-app`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system survey-quiz-app && \
          adduser --system -G survey-quiz-app survey-quiz-app

COPY dist/apps/survey-quiz-app survey-quiz-app/
RUN chown -R survey-quiz-app:survey-quiz-app .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix survey-quiz-app --omit=dev -f install

CMD [ "node", "survey-quiz-app" ]
